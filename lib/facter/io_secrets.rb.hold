# TODO do we set default facts this way, so we know they are always set
# then just drive off if they are default or not?
Facter.add(:io_secrets_id) do
  setcode do
    'none'
  end
end
Facter.add(:io_secrets_group) do
  setcode do
    'none'
  end
end
Facter.add(:io_secrets_prefix) do
  setcode do
    'none'
  end
end
Facter.add(:io_secrets_suffix) do
  setcode do
    'none'
  end
end
